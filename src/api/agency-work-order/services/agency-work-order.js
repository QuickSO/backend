'use strict';

/**
 * agency-work-order service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::agency-work-order.agency-work-order');
