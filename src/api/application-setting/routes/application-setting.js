'use strict';

/**
 * application-setting router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::application-setting.application-setting');
