'use strict';

/**
 * vendor-debit-note service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::vendor-debit-note.vendor-debit-note');
