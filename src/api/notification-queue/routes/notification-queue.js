'use strict';

/**
 * notification-queue router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::notification-queue.notification-queue');
