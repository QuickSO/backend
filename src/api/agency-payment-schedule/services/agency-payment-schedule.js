'use strict';

/**
 * agency-payment-schedule service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::agency-payment-schedule.agency-payment-schedule');
