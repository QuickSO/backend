'use strict';

/**
 *  agency-payment-schedule controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::agency-payment-schedule.agency-payment-schedule');
