'use strict';

/**
 * agency-payment-schedule router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::agency-payment-schedule.agency-payment-schedule');
