'use strict';

/**
 * reminder-template service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::reminder-template.reminder-template');
