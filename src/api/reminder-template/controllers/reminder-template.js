'use strict';

/**
 *  reminder-template controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::reminder-template.reminder-template');
