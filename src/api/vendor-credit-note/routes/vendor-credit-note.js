'use strict';

/**
 * vendor-credit-note router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::vendor-credit-note.vendor-credit-note');
