'use strict';

/**
 * vendor-credit-note service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::vendor-credit-note.vendor-credit-note');
