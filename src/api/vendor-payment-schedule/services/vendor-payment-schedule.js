'use strict';

/**
 * vendor-payment-schedule service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::vendor-payment-schedule.vendor-payment-schedule');
