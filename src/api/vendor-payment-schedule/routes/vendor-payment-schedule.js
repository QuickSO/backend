'use strict';

/**
 * vendor-payment-schedule router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::vendor-payment-schedule.vendor-payment-schedule');
