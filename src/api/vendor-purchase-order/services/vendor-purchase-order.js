'use strict';

/**
 * vendor-purchase-order service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::vendor-purchase-order.vendor-purchase-order');
