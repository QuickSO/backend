'use strict';

/**
 * budget-tracker-phase router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::budget-tracker-phase.budget-tracker-phase');
