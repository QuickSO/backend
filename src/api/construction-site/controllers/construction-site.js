'use strict';

/**
 *  construction-site controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::construction-site.construction-site');
