'use strict';

/**
 * construction-site service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::construction-site.construction-site');
