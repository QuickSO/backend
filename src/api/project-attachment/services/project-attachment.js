'use strict';

/**
 * project-attachment service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::project-attachment.project-attachment');
