'use strict';

/**
 * inventory-setting service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::inventory-setting.inventory-setting');
