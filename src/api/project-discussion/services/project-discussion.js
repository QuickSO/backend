'use strict';

/**
 * project-discussion service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::project-discussion.project-discussion');
