'use strict';

/**
 *  agency-requirement-form controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::agency-requirement-form.agency-requirement-form');
