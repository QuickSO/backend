'use strict';

/**
 * agency-requirement-form service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::agency-requirement-form.agency-requirement-form');
