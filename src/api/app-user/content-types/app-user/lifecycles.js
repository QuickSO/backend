  // module.exports = {
  //   async beforeCreate(event) {
  //     const userCount = await strapi.db.query('api::app-user.app-user').count();

  //     if (userCount >= 10) {
  //       throw new Error('User limit reached (max 10 users allowed).');
  //     }
  //   },
  // };
