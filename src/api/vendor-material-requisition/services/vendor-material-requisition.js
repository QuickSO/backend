'use strict';

/**
 * vendor-material-requisition service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::vendor-material-requisition.vendor-material-requisition');
