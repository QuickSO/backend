'use strict';

/**
 * vendor-extra-work controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::vendor-extra-work.vendor-extra-work');
