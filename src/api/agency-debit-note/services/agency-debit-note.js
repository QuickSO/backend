"use strict";

/**
 * agency-debit-note service.
 */

const { createCoreService } = require("@strapi/strapi").factories;

module.exports = createCoreService("api::agency-debit-note.agency-debit-note");
